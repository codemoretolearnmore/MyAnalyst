# app/__init__.py
# This file ensures `app` is treated as a package.

import os
from dotenv import load_dotenv

# Load environment variables from .env file
load_dotenv()

# You can import configurations or setup initialization logic here if needed
